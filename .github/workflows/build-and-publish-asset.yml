name: Build, Publish and Release
run-name: ${{ github.actor }} is building, publishing and releasing 
on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    types:
      - closed

jobs:
  call-asset-build:
    if: github.event.pull_request.merged == true
    uses: luitjens-test-gh-app/workflows/.github/workflows/asset-build-and-publish.yml@8c5e427ea45b768b76873a7b7e969e54e6bf7ae7
    secrets: inherit
